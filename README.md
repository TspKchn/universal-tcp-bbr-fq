# Universal TCP BBR + fq Manager (ภาษาไทย)

สคริปต์ **Universal แบบ Standalone** สำหรับจัดการ **TCP BBR + fq** บน VPS  
ออกแบบมาเพื่อ **ลด Latency, เล่นเกม, และใช้งาน VPN อย่างเสถียร**

สคริปต์นี้ **ไม่ผูกกับ VPN ใด ๆ** และสามารถใช้ร่วมกับสคริปต์หรือระบบใดก็ได้  
รองรับทั้ง **Debian และ Ubuntu**

---

## ✨ คุณสมบัติ

- เปิดใช้งานและ **ล็อค TCP BBR**
- บังคับใช้ **fq queue discipline** (ป้องกัน `cake`)
- เมนูใช้งานแบบสคริปต์เซิร์ฟเวอร์ (สไตล์ ShowOn)
- มีเมนู Install / Uninstall / Status Check
- ใช้งานได้กับ **ทุกสคริปต์ VPN**
  - Xray / VLESS / VMess
  - SSH / SSL / SNI
  - OpenVPN / WireGuard
  - Hysteria / TUIC
- **ไม่เปลี่ยน kernel**
- ปลอดภัยสำหรับ VPS ใช้งานจริง
- เลือก reboot หลังติดตั้งได้

---

## 🖥 ระบบปฏิบัติการที่รองรับ

- Debian 9 / 10 / 11 / 12
- Ubuntu 18.04 / 20.04 / 22.04 / 24.04

---

## 🚀 วิธีติดตั้งและใช้งาน

### วิธีที่ 1: รันไฟล์โดยตรง
```bash
chmod +x universal-tcp-bbr-fq.sh
./universal-tcp-bbr-fq.sh
```

### วิธีที่ 2: One-liner (แนะนำ)
```bash
curl -fsSL https://raw.githubusercontent.com/TspKchn/universal-tcp-bbr-fq/main/universal-tcp-bbr-fq.sh | bash
```

> ต้องรันด้วย **root**

---

## 📋 หน้าตาเมนู

```
=================================================
   Universal TCP BBR + fq Manager
=================================================
 1. Install TCP BBR (Lock BBR + fq)
 2. Uninstall TCP BBR (Restore Default)
 3. Status Check
 0. Exit
-------------------------------------------------
```

---

## 🔧 สคริปต์นี้ทำอะไรบ้าง

### Install TCP BBR
- โหลด kernel module `tcp_bbr`
- ตั้งค่า
  - `net.ipv4.tcp_congestion_control = bbr`
  - `net.core.default_qdisc = fq`
- สร้างไฟล์ sysctl สำหรับล็อคค่า
- สร้าง systemd service เพื่อบังคับค่าเดิมทุกครั้งที่บูต
- เลือก reboot หลังติดตั้งได้

### Uninstall TCP BBR
- ลบไฟล์ sysctl ที่ใช้ล็อค
- ปิดและลบ systemd service
- คืนค่า default ที่ปลอดภัย
  - `cubic`
  - `fq_codel`
- แนะนำ reboot หลังถอนการติดตั้ง

### Status Check
แสดงสถานะ:
- TCP congestion control ปัจจุบัน
- qdisc ที่ใช้อยู่
- สถานะ module `tcp_bbr`
- การมีอยู่ของไฟล์ lock
- สถานะ systemd service

---

## 🎮 เหมาะกับใคร

- VPS สำหรับเล่นเกม (ROV, MOBA, FPS)
- เซิร์ฟเวอร์ VPN ทุกประเภท
- ลูกค้าที่ใช้งานเครือข่ายมือถือทุกค่าย
- VPS ที่ต้องการ **ปิงนิ่ง ไม่สวิง**
- เครื่องที่ไม่ต้องการให้ `cake` ถูกตั้งค่าอัตโนมัติ

---

## ⚠ หมายเหตุสำคัญ

- สคริปต์นี้ **ไม่แก้ kernel**
- ไม่ยุ่งกับ
  - firewall
  - iptables / nftables
  - config VPN ใด ๆ
- ใช้งานร่วมกับระบบเดิมได้ทันที
- ออกแบบมาเพื่อ **ความนิ่งของ latency** ไม่ใช่การเร่ง bandwidth

---

## 📄 License

MIT License  
ใช้งาน แก้ไข และแจกจ่ายได้อย่างอิสระ

---

## 🤝 การมีส่วนร่วม

สามารถเปิด Issue หรือ Pull Request เพื่อปรับปรุงสคริปต์ได้  
เหมาะสำหรับใช้เป็น **Baseline มาตรฐานของ VPS ทุกเครื่อง**
